# Core
OPENAI_API_KEY="REPLACE_ME"
OPENAI_MODEL="gpt-4.1-mini"
KB_PATH="./kb/example.yaml"

# Provider endpoints (examples)
LOG_STORE_URL="http://LOG_STORE_HOST:PORT"
LOG_STORE_TOKEN=""

VCS_TOKEN="REPLACE_ME"          # e.g., GitHub token
DEPLOY_TOKEN="REPLACE_ME"        # can reuse VCS_TOKEN if same system

# Optional: if you use a unified token
# PROVIDER_TOKEN="REPLACE_ME"